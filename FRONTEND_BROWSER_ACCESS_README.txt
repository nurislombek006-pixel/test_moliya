ФРОНТЕНД-ONLY БРАУЗЕРНЫЙ ВХОД

Что сделано:
- Вход через старый localStorage-ключ quizBrowserAdminAuth_micro_v1 отключён.
- Для браузера добавлен экран с личным кодом доступа.
- Сессия хранится отдельным подписанным токеном, а не старым ключом browser_admin.
- Cloudflare Worker и серверные файлы убраны.

Код доступа по умолчанию:
Sayfiddinov-2026

Как поменять код:
1) Открой micro_script.js.
2) Найди FRONTEND_BROWSER_ACCESS_CODE_HASHES.
3) Замени хэш на хэш своего нового кода.

Важно:
Это фронтенд-only ограничение. Его нельзя считать настоящей защитой, потому что JS-файл доступен пользователю. Оно закрывает простой DevTools-хак через localStorage, но сильный пользователь всё равно может обойти защиту, если изучит код.
